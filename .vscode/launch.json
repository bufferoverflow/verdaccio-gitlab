{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch verdaccio",
            "program": "${workspaceFolder}/lib/verdaccio.js",
            "preLaunchTask": "npm: build",
            "env": {
                "NODE_PATH": "${workspaceFolder}/..",
                "NODE_ENV": "development"
            }
        }
    ]
}
